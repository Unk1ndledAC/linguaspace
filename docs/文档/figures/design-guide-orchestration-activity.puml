@startuml
skinparam dpi 160
skinparam shadowing false
title 智能导览当前编排活动图
start
:接收文本、语音或图片输入;
if (语音输入?) then (是)
  :faster-whisper 转写;
elseif (图片输入?) then (是)
  :Ollama Vision 生成图片摘要;
endif
:术语表归一化;
:应用层哈希向量检索已发布知识;
if (达到可靠阈值?) then (是)
  :查询文化关系;
  :构造受控提示词;
  :调用 Ollama 或 OpenAI-compatible Provider;
  :保存消息、模型日志和请求追踪;
else (否)
  :返回“暂无可靠资料”;
  :保存未命中请求追踪;
endif
stop
@enduml
