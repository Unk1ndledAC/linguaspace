@startuml
skinparam dpi 160
skinparam shadowing false
title 真人导游协同当前活动图
start
:游客会话持续写入消息和画像;
:导游查看会话列表与上下文;
if (需要人工介入?) then (是)
  :游客提交 handoff 请求或导游主动接管;
  :会话状态更新为 taken_over;
  :记录 takeover_logs;
  :导游发送人工回复;
  if (完成服务?) then (是)
    :释放接管并恢复 active;
  endif
endif
if (导游提交修正?) then (是)
  :创建 guide_corrections;
  :创建 pending 审核任务;
endif
stop
@enduml
